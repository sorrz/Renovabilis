import { Container, Typography } from '@mui/material';

export default function MapView() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>MapView</Typography>
      <Typography>This is the MapView page.</Typography>
    </Container>
  );
}
